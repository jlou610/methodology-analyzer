# Copy to .env for local development.
# Local dev uses ./data/app.db; production sets DB_PATH to the Render disk mount.
SECRET_KEY=change-me-to-a-random-string
# DB_PATH=./data/app.db
# Used by the Weekend 2 analyzer:
# ANTHROPIC_API_KEY=sk-ant-...
